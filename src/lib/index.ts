import { createCookie, deleteCookie, getCookie } from './actions'
import {
  createLocalStorage,
  deleteLocalStorage,
  getLocalStorage,
} from './localStorage'

export {
  createCookie,
  deleteCookie,
  getCookie,
  createLocalStorage,
  deleteLocalStorage,
  getLocalStorage,
}
